<% provide(:title, "Users") %>

<ul>
  <% for user in @users %>
  <li>
    <span><%= user.username %></span> -
    <span><%= user.email %></span>
    <%= link_to 'Delete', user_path(user.id), method: :delete %>
  </li>
  <% end %>
</ul>